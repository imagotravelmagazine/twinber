[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  # Questa impostazione è cruciale: dice a Netlify di ignorare le stringhe "AIza..." trovate
  # nei file delle librerie (node_modules), permettendo al build di completarsi.
  SECRETS_SCAN_SMART_DETECTION_ENABLED = "true"
  SECRETS_SCAN_SMART_DETECTION_OMIT_VALUES = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
